---
import "../assets/global.css";
import { Menu } from "@components/Menu";
import i18next, { t } from "i18next";

const { titleKey, wrapperClass } = Astro.props;
---

<html lang={i18next.language}>
  <head>
    <meta charset="utf-8" />
    <title>🚀{titleKey ? t(titleKey) : "Admin"}💸</title>
    <meta name="viewport" content="width=device-width" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="flex flex-row bg-gray-50 2xl:max-w-7xl mx-auto font-montserrat">
    <Menu client:load />
    <section class="w-full h-full pr-8">
      <div class="h-4 w-full top-0 sticky backdrop-blur-sm"></div>
      <nav class="px-8 items-center flex z-50 card h-16 transition-all top-4 sticky">
        <h1 class="font-bold text-2xl">
          {titleKey ? t(titleKey) : "Admin"}
        </h1>
      </nav>
      <article class={`container mt-8 ${wrapperClass}`}>
        <slot />
      </article>
    </section>
  </body>
</html>
